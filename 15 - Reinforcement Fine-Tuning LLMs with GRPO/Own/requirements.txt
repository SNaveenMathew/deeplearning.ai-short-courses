# ──────────────────────────────────────────────────────────────────────────────
# Wordle GRPO Training — Python Dependencies
# Hardware: NVIDIA RTX 4090 (CUDA 12.x)
# ──────────────────────────────────────────────────────────────────────────────

# 1. Install PyTorch FIRST (with CUDA 12.1 — adjust if your driver is older):
#    pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# 2. Then install the rest:
#    pip install -r requirements.txt

# ── Core ML stack ─────────────────────────────────────────────────────────────
transformers>=4.45.0        # model loading, tokenizers, chat templates
trl>=0.12.0                 # GRPOTrainer + GRPOConfig
peft>=0.13.0                # LoRA / parameter-efficient fine-tuning
accelerate>=0.34.0          # device placement, mixed precision utilities
datasets>=2.20.0            # Dataset class used for train/eval sets

# ── Quantisation (optional — not needed for 1.5B on a 4090, but useful
#    if you upgrade to 7B+ and want 4-bit loading via load_in_4bit=True) ──────
bitsandbytes>=0.43.0

# ── Experiment tracking (optional — set report_to="wandb" in TrainConfig) ────
# wandb

# ── Utilities ─────────────────────────────────────────────────────────────────
numpy>=1.24.0
packaging
